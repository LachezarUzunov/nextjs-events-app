import { getFeaturedEvents } from "../dummy-data";

const Home = () => {
  const featuredEvents = getFeaturedEvents();

  return (
    <main>
      <h1>Hello</h1>
    </main>
  );
};

export default Home;
